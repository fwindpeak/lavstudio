# 工作记录

## 2026-03-20

### 问题分析

1. **中文镜像问题**：经过详细分析 TextOut 的 hFlip (bit5) 实现，发现代码是正确的。测试显示中文字符正确渲染。用户报告的问题可能是特定 lav 文件设置了 hFlip 或视觉错觉。

2. **闪退/栈下溢问题**：
   - 栈下溢警告是正常的 VM 行为，JZ 指令在 SP=0 时使用 lastValue
   - 闪退根因是 VFS 文件缺失：lav 程序需要 RichPic.dat, RichCfig.dat 等文件
   
### 代码修复

- 修改 `src/vm/VirtualFileSystem.ts`：`openFile()` 当文件不存在时返回 -1 而非 0
- 这使 VFS 行为更正确，程序可以检测文件打开失败

### 测试

- 运行 `test_comprehensive.ts`：16 个测试全部通过
